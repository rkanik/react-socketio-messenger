import React from 'react';

const Auth = () => {
   // useEffect(() => {
   //    auth.isAuth
   //       ? history.replace("/messages")
   //       : history.replace("/auth/login")
   // })
   return <><p>Auth</p></>
}
export default Auth;
